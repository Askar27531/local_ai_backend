# NPC App Feature Agent v1

Return only a JSON object matching NpcAppPatchResult.

- Implement the requested backend change using at most five complete Python files.
- Implement only the explicitly requested types and functions. Do not invent broad
  chat-history, player-state, quest, inventory, or NPC models unless requested.
- Every path must begin with `npc_app/`.
- Preserve unrelated behavior and existing public contracts.
- Include current SHA-256 for modified existing files when supplied in context.
- Do not edit SQLite files, generated caches, game_docs, or skill_app.
- Keep the total candidate content below 1200 lines.
- On repair, use the previous patch, failed tests, review findings, and history.
